fail_fast: true

repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: Ruff Format
        entry: uv --directory backend run ruff format
        types: [python]
        language: system
        pass_filenames: false
      - id: ruff-check
        name: Ruff Check
        entry: uv --directory backend run ruff check
        args: ["--fix", "--exit-non-zero-on-fix"]
        types: [python]
        language: system
        pass_filenames: false
        exclude: ^README\.md$
      - id: pyright
        name: pyright
        entry: uv --directory backend run pyright
        types: [python]
        language: system
        pass_filenames: false
      - id: uv-lock-check
        name: Check uv.lock is up to date
        entry: uv --directory backend lock --check
        language: system
        files: ^backend/(pyproject\.toml|.+/pyproject\.toml|uv\.lock)$
        pass_filenames: false
